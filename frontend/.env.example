# Frontend Environment Variables

# API Base URL (defaults to /api, proxied by Vite to backend)
VITE_API_URL=/api

# WebSocket URL (optional - leave unset in dev, derived from window.location.host)
# Set in production only: VITE_WS_URL=wss://yourserver.com/claude-chat
